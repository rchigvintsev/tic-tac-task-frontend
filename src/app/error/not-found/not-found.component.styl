.error-container {
  display: flex;
  flex-direction: row;
  justify-content: center;

  .error-subcontainer {
    background-color: #ffffff;
    box-shadow: 10px 5px 3px #cecece;
    border-radius: 4px;
    text-transform: uppercase;
    padding: 0 48px 48px 48px;
    max-width: 640px;

    .error-code {
      display: flex;
      flex-direction: row;
      justify-content: center;

      h1 {
        font-size: 200px;
        margin: 0;
        font-weight: 900;
        color: transparent;
        background-color: #cecece;
        background-clip: text;
        text-shadow: 10px 5px 3px rgba(255, 255, 255, 0.8);
      }

      @media (max-width: 480px) {
        h1 {
          font-size: 100px;
          text-shadow: 5px 3px 2px rgba(255, 255, 255, 0.8);
        }
      }
    }

    .error-text {
      display: flex;
      flex-direction: column;

      h2 {
        font-size: 20px;
        font-weight: 900;
        margin: 0;
        text-align: center;
      }

      p {
        font-size: 16px;
        font-weight: 400;
        margin-top: 16px;
        text-align: center;
      }
    }

    .button-container {
      display: flex;
    }
  }
}

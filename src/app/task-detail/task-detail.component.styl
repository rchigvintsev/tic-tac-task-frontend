mat-card {
  form {
    display: flex;
    flex-direction: column;
  }

  .mat-card-header {
    a {
      margin-bottom: 8px;
    }

    .mat-card-title {
      margin-top: 11px;
      margin-bottom: 14px;
      margin-left: 8px;
      flex-grow: 1;
    }
  }

  mat-card-content {
    form {
      font-size: 16px;
    }
  }
}

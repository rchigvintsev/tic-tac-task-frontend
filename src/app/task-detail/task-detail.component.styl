mat-card {
  form {
    display: flex;
    flex-direction: column;
  }

  .mat-card-header {
    a {
      margin-bottom: 8px;
    }

    .mat-card-title {
      margin-top: 10px;
      margin-bottom: 15px;
      margin-left: 8px;
      flex-grow: 1;
    }
  }
}
